# Configuração de Produção para Railway
APP_NAME="Projeto ExpoAgro"
APP_ENV=production
APP_KEY=base64:LL2VO9PORsTIjJ+MnJP3jnA4D1E7UhiVbJtZMMMizoo=
APP_DEBUG=false
APP_URL=https://projetoexpoagro-production-up.railway.app
APP_TIMEZONE=America/Sao_Paulo

# Configuração de Log
LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

# Banco de Dados - Usando variáveis do Railway
DB_CONNECTION=mysql
DB_HOST=${MYSQLHOST}
DB_PORT=${MYSQLPORT}
DB_DATABASE=${MYSQLDATABASE}
DB_USERNAME=${MYSQLUSER}
DB_PASSWORD=${MYSQLPASSWORD}

# Alternativa usando URL completa (mais simples)
# DATABASE_URL=${MYSQL_URL}

# Configurações de Cache e Session para Produção
BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

# Configurações de Email (opcional)
MAIL_MAILER=log
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# Configurações AWS (se necessário)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# Configurações Pusher (se necessário)
PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

# Configurações Vite para Produção
VITE_APP_NAME="${APP_NAME}"

## 🎯 Sim! Você Pode Usar Todas Essas Variáveis do Railway!

Perfeito! Vejo que o Railway fornece várias variáveis do sistema que você pode aproveitar. Vou mostrar como usar cada uma delas:

## 📊 **Variáveis Railway Disponíveis na Imagem:**

### **🔧 Variáveis de Sistema Railway:**
- ✅ `RAILWAY_PUBLIC_DOMAIN` - Domínio público do serviço
- ✅ `RAILWAY_PRIVATE_DOMAIN` - Domínio privado interno
- ✅ `RAILWAY_PROJECT_NAME` - Nome do projeto
- ✅ `RAILWAY_ENVIRONMENT_NAME` - Nome do ambiente (production)
- ✅ `RAILWAY_SERVICE_NAME` - Nome do serviço
- ✅ `RAILWAY_PROJECT_ID` - ID único do projeto
- ✅ `RAILWAY_ENVIRONMENT_ID` - ID do ambiente
- ✅ `RAILWAY_SERVICE_ID` - ID do serviço

## 🚀 **Como Usar no .env.production:**
```
# Configuração de Produção para Railway - Usando Variáveis Dinâmicas
APP_NAME="${RAILWAY_PROJECT_NAME}"
APP_ENV=${RAILWAY_ENVIRONMENT_NAME}
# APP_KEY=base64:LL2VO9PORsTIjJ+MnJP3jnA4D1E7UhiVbJtZMMMizoo= <- REMOVER ESTA LINHA
APP_DEBUG=false
APP_URL=https://${RAILWAY_PUBLIC_DOMAIN}
APP_TIMEZONE=America/Sao_Paulo

# Informações do Railway para logs/debug
RAILWAY_PROJECT_NAME=${RAILWAY_PROJECT_NAME}
RAILWAY_SERVICE_NAME=${RAILWAY_SERVICE_NAME}
RAILWAY_ENVIRONMENT=${RAILWAY_ENVIRONMENT_NAME}
RAILWAY_PROJECT_ID=${RAILWAY_PROJECT_ID}

# Banco de Dados - Usando variáveis MySQL do Railway
DB_CONNECTION=mysql
DB_HOST=${MYSQLHOST}
DB_PORT=${MYSQLPORT}
DB_DATABASE=${MYSQLDATABASE}
DB_USERNAME=${MYSQLUSER}
DB_PASSWORD=${MYSQLPASSWORD}

# Configurações de Cache e Session
CACHE_DRIVER=file
SESSION_DRIVER=file
QUEUE_CONNECTION=sync
FILESYSTEM_DISK=local

# Configurações de Log
LOG_CHANNEL=stack
LOG_LEVEL=error

# Email (usando log para produção)
MAIL_MAILER=log
MAIL_FROM_ADDRESS="noreply@${RAILWAY_PUBLIC_DOMAIN}"
MAIL_FROM_NAME="${RAILWAY_PROJECT_NAME}"

# Configurações Vite
VITE_APP_NAME="${RAILWAY_PROJECT_NAME}"
VITE_APP_ENV=${RAILWAY_ENVIRONMENT_NAME}
```