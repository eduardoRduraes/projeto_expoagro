<IfModule mod_rewrite.c>
    RewriteEngine on
    RewriteCond %{HTTP_HOST} ^projetoerpagricola.kinghost.net$ [NC,OR]
    RewriteCond %{HTTP_HOST} ^www.projetoerpagricola.kinghost.net$
    RewriteCond %{REQUEST_URI} !public/
    RewriteRule (.*) /public/$1 [L]
</IfModule>
